<?php
/**
 * Copy to config.local.php and fill in. Do not commit config.local.php.
 */

declare(strict_types=1);

return [
    'app' => [
        'env' => 'production',
        'debug' => false,
        'url' => 'https://YOUR_DOMAIN',
        'name' => 'Sunview Development and Consultancy (SDC)',
        'timezone' => 'America/Denver',
    ],

    'db' => [
        'host' => 'localhost',
        'port' => 3306,
        'name' => 'YOUR_DB_NAME',
        'user' => 'YOUR_DB_USER',
        'pass' => 'YOUR_DB_PASSWORD',
        'charset' => 'utf8mb4',
    ],

    'mail' => [
        // Prefer: driver=brevo with API key. Or driver=smtp with PHPMailer SMTP below.
        // If driver=brevo but API key is empty, SMTP is used automatically when smtp_host is set.
        'driver' => 'brevo',
        'from_email' => 'noreply@YOUR_DOMAIN',
        'from_name' => 'SDC',
        'admin_notify_email' => 'leads@YOUR_DOMAIN',
        'brevo_api_key' => 'YOUR_BREVO_API_KEY',
        // PHPMailer SMTP (Hostinger / cPanel / Google / Brevo SMTP login, etc.)
        'smtp_host' => 'smtp.YOUR_HOST',       // e.g. smtp.hostinger.com or smtp-relay.brevo.com
        'smtp_port' => 587,
        'smtp_encryption' => 'tls',            // tls (587) or ssl (465)
        'smtp_user' => 'YOUR_SMTP_USERNAME',
        'smtp_pass' => 'YOUR_SMTP_PASSWORD',
    ],

    'security' => [
        'cookie_secure' => true,
        'trust_forwarded_proto' => false,
        'health_token' => 'CHANGE_ME_TO_A_LONG_RANDOM_TOKEN',
        'login_max_attempts' => 5,
        'login_lockout_seconds' => 900,
        'inquiry_max_per_hour' => 5,
    ],

    'smartsupp' => [
        'key' => 'YOUR_SMARTSUPP_KEY',
    ],
];
